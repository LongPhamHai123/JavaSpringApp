server.port=8080

H2 Database Configuration (for development)
spring.datasource.url=jdbc:h2:mem:authdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (access at http://localhost:8080/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.example.auth=DEBUG


# MongoDB
# spring.mongodb.uri=mongodb://localhost:27017/authdb

# # Tell Spring to use MongoDB for HTTP session
# spring.session.store-type=mongodb
# # (Optional) session timeout

# # Session timeout (30 minutes)
# server.servlet.session.timeout=30m

# JWT Configuration
spring.app.jwtSecret=8391b8228265cd86984b538ae6b2b98d2564ae89619aabf6d6b3030bfa684438
spring.app.jwtExpirationInMs=300000

